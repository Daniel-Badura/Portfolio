<% layout('layouts/boilerplateWidoczki') %>

        <h1> Wszystkie Widoczki</h1>

        <div>
            <!-- <a href="/widoczki/new">Dodaj Widoczek</a> -->
        </div>

        <% for (let widoczek of widoczki) {%>
            <div class="card">
                <div class="row">
                    <div class="col-md-4">
                        <img class="img-fluid" style=" height: 300px;
                        height: auto;" src="<%= widoczek.image %>">

                        </a>
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= widoczek.name %>
                            </h5>
                            <p class="card-text">
                                <%= widoczek.description %>
                            </p>
                            <p class="card-text">
                                <small class="text-muted">
                                    <%= widoczek.location %>
                                </small>
                            </p>
                            <a class="btn btn-primary" href="/widoczki/<%= widoczek._id %>">Przejdź do <%= widoczek.name
                                    %></a>
                        </div>
                    </div>
                </div>
            </div>
            <% }%>